<!DOCTYPE html>
<html lang="es" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/812c8ee19a.js" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <link rel="stylesheet" th:href="@{/css/operadores.css}">
    <title>YQO - Operadores</title>
</head>
<body>
<div class="d-flex">
    <!-- Sidebar -->
    <div class="sidebar">
        <a href="#" class="">
            <img th:src="@{/img/logo.svg}" class="logo" alt="Logo">
        </a>
        <hr>
        <ul class="list">
            <li>
                <a th:href="@{/home}" class="">
                    <img th:src="@{/img/casa1.svg}" alt="Icono" class="icon">
                    Inicio
                </a>
            </li>
            <li>
                <a th:href="@{/operators}" class="">
                    <img th:src="@{/img/grafico.png}" alt="" class="icon">
                    Reportes
                </a>
            </li>
            <li>
                <a th:href="@{/operators}" class="">
                    <img th:src="@{/img/user.png}" alt="" class="icon">
                    Operadores
                </a>
            </li>
            <li>
                <a th:href="@{/operators}" class="">
                    <img th:src="@{/img/solicitud.png}" alt="" class="icon">
                    Solicitudes
                </a>
            </li>
            <li>
                <a th:href="@{/operators}" class="">
                    <img th:src="@{/img/help.png}" alt="" class="icon">
                    Ayuda
                </a>
            </li>
            <li>
                <a th:href="@{/operators}" class="">
                    <img th:src="@{/img/config.png}" alt="" class="icon">
                    Configuración
                </a>
            </li>
        </ul>
        <hr>
        <div>
            <a th:href="@{/operators}" class="">
                <img th:src="@{/logout.png}" alt="" class="icon">
                Logout
            </a>
        </div>
    </div>

    <div class="main-content flex-grow-1">
        <nav class="navbar">
            <div class="bars-item">
                <form class="d-flex">
                    <input class="form-control" type="search" placeholder="Búsqueda" aria-label="Search">
                </form>
                <div class="d-flex align-items-center">
                    <i class="fa-regular fa-bell campana"></i>
                    <img th:src="@{/img/op1.jpg}" alt="User" class="profile">
                </div>
            </div>
        </nav>

        <div class="content">
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Correo</th>
                    <th>Teléfono</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
                </thead>
                <tbody>
                <tr th:each="operator : ${operators}">
                    <td th:text="${operator.id}"></td>
                    <td th:text="${operator.name}"></td>
                    <td th:text="${operator.email}"></td>
                    <td th:text="${operator.cellphone}"></td>
                    <td>
                        <span th:text="${operator.status ? 'ACTIVO' : 'INACTIVO'}" 
                              th:class="${operator.status ? 'text-success' : 'text-warning'}"></span>
                    </td>
                    <td>
                        <a th:href="@{/operators/editar/{id}(id=${operator.id})}" class="btn btn-sm btn-warning">
                            <i class="fas fa-edit"></i>
                        </a>
                        <a th:href="@{/operators/eliminar/{id}(id=${operator.id})}" class="btn btn-sm btn-danger">
                            <i class="fas fa-trash"></i>
                        </a>
                    </td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>


</body>
</html>